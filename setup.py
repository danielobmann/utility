from setuptools import setup

setup(name="utility",
      version="0.0.1",
      description="Utility functions for training networks.",
      packages=["utility"],
      install_requires=['tensorflow', 'numpy'])
